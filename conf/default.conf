server {
    listen 80;
    server_name _;

    # Next.js 컨테이너 → 기본 라우팅
    location / {
        proxy_pass http://frontend:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    # API 프록시 → 백엔드 EC2:8080
    location /api/ {
        proxy_pass http://52.79.241.142:8080;   # 백엔드 IP/도메인:포트
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    # ✅ WebSocket/STOMP 프록시 → 반드시 /ws-stomp 로 일치
    location /ws-stomp {
        proxy_pass http://52.79.241.142:8080/ws-stomp;
        proxy_http_version 1.1;

        # 업그레이드 필수
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;

        proxy_read_timeout 600s;
        proxy_send_timeout 600s;
    }

  # S3의 public 객체를 동일 오리진(/uploads/)으로 프록시
  location /uploads/ {
      # 🔹 슬래시(/) 중요: /uploads/xxx → S3의 /xxx 로 매핑
      proxy_pass https://daepa-s3.s3.ap-northeast-2.amazonaws.com/;

      proxy_http_version 1.1;

      # 🔹 SNI/Host 헤더를 S3 버킷 도메인으로 맞춰야 함
      proxy_set_header Host daepa-s3.s3.ap-northeast-2.amazonaws.com;
      proxy_ssl_server_name on;

      # (선택) 원본 클라이언트 정보
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-Proto $scheme;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;

      # (선택) 큰 이미지/동영상 재생 시 유리
      proxy_read_timeout 600s;
      proxy_send_timeout 600s;
      proxy_force_ranges on;

      # (선택) 캐시 헤더 전달/강제
      expires 7d;
      add_header Cache-Control "public, max-age=604800, immutable";
  }

}
